var extractJwt=passport_jwt.ExtractJwt,jwtStrategy=passport_jwt.Strategy;console.dir(passport_jwt),"mongo"==global.authMeth?(module.exports=(passport=>{var opts={};opts.jwtFromRequest=extractJwt.fromAuthHeader(),opts.secretOrKey=config.secret,passport.use(new jwtStrategy(opts,(jwt_payload,done)=>{member.findOne({id:jwt_payload.id},(err,user)=>{if(err)return done(err,!1);done(null,user||!1)})}))}),c_log("\nâœ”".succ+" Loaded member authentication via mongodb.\n".info)):(module.exports=(passport=>{var opts={};opts.jwtFromRequest=extractJwt.fromAuthHeader(),opts.secretOrKey=config.secret,passport.use(new jwtStrategy(opts,(jwt_payload,done)=>{$connection.query("SELECT * FROM vw_members WHERE member_id=$1 AND email=$2 AND role=$3 AND telephone=$4 AND active=$5",[jwt_payload.member_id,jwt_payload.email,jwt_payload.role,jwt_payload.telephone,1]).then(function(user){done(null,!!user&&user[0])}).catch(function(err){return done(err,!1)})}))}),c_log("\nâœ”".succ+" Loaded member authentication via sql.\n".info));